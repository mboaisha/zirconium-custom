modules:
  - type: dnf
    repos:
      cleanup: true
      files:
        - vscodium.repo
      keys:
        - https://gitlab.com/paulcarroty/vscodium-deb-rpm-repo/raw/master/pub.gpg
    install:
      packages:
        - codium
